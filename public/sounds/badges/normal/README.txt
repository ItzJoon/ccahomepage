이 폴더에 "normal" 등급 뱃지 획득 효과음 파일을 넣어주세요.

1) 공통 사운드 (필수, 기본값): default.mp3
   - 이 폴더의 모든 뱃지가 기본으로 재생할 파일입니다. 반드시 이 이름으로 하나는 넣어주세요.

2) 뱃지별 개별 사운드 (선택):
   - 파일명을 뱃지의 code 값과 동일하게 지으면(예: streak_3.mp3, phantom_member.mp3)
     해당 뱃지를 획득했을 때만 그 파일이 재생되고, 없으면 자동으로 default.mp3가 재생됩니다.
   - 뱃지 code 값은 /admin/badges 화면 또는 DB의 badges.code 컬럼에서 확인할 수 있습니다.

형식: mp3 권장(다른 브라우저 지원 포맷도 가능하나 확장자는 .mp3로 통일해야 코드가 인식합니다).
