# CLAUDE.md

이 저장소에서 작업할 때 따라야 하는 규칙입니다.

## GitHub Issue 관리 규칙

- 작업을 시작하기 전, 관련 GitHub Issue가 있는지 `gh issue list`로 확인해라
- 기존 이슈에 없는 새로운 작업 요청을 받으면, 작업 시작 전에 `gh issue create`로 이슈를 먼저 만들어라
  (제목은 요청 내용 요약, 라벨은 기존 라벨 중 맞는 것 선택 또는 없으면 새로 생성)
- 사소한 버그 수정이나 UI 조정은 새 이슈를 만들지 말고, 관련된 기존 이슈에 댓글로 남겨라
- 작업이 끝나면 관련 이슈를 `gh issue close <번호> --comment "완료 내용 요약"`으로 닫아라
- 커밋 메시지에도 관련 이슈 번호를 `Closes #번호` 형식으로 포함해라
- 작업 중간에 스코프가 바뀌거나 하위 작업이 추가되면, 코멘트로 이슈에 남겨라 (`gh issue comment <번호> --body "..."`)

## 개인정보가 담긴 파일

- 실제 학생/교사의 이름·이메일 등 개인정보가 담긴 파일(예: `supabase/seed_directory.sql`)은
  절대 git에 커밋하지 않는다. `.gitignore`에 등록해서 추적 자체가 안 되게 하고, 내용은
  DB 테이블 구조를 파악하는 용도로만 참고하고 `git add`/커밋 대상에는 포함시키지 않는다.
